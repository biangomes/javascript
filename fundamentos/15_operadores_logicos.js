let [a,b,c] = [1,2,3];

a++;    // operador post fixed (pos operando)
++a;    // operador pre fixed (antes do operando)


const soma = a+b;   // operador in fixed (entre o operando)
const sub = a-b;
const mult = a*b;
const div = a/b;
const mod = a % b;
const potencia = a ** b;
